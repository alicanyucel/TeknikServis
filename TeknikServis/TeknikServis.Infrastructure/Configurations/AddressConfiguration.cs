namespace TeknikServis.Infrastructure.Configurations;

// Address is a value object represented by scalar properties on owning entities (e.g., Customer.AddressLine, City, Neighborhood, District, ZipCode).
// No separate EF Core entity or owned-type configuration is required here. This placeholder avoids EF trying to map strings as entities.
internal static class AddressConfiguration
{
}